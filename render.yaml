services:
  - type: web
    name: eggzilla
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_PIN
        sync: false # Will prompt you to set this in Render dashboard
